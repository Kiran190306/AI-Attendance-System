# requirements for AI Attendance System (Flask version)

Flask==2.3.3
Flask-Cors==3.0.10
SQLAlchemy==2.0.20

# HTTP
requests==2.31.0

# Environment
python-dotenv==1.0.0

# Database (PostgreSQL)
psycopg2-binary==2.9.6

# Image / ML
numpy==1.25.2
Pillow==10.0.1
opencv-python==4.8.0.76
face-recognition==1.3.0

# Production web server
gunicorn==20.1.0

# Optional accelerated libraries (install only if available on platform)
# dlib and mediapipe are optional and may be installed on supported platforms

